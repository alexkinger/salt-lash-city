# Public (Vite)
VITE_TURNSTILE_SITE_KEY=

# Server-only (Vercel env vars — never prefix with VITE_)
TURNSTILE_SECRET_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
RESEND_API_KEY=
LEAD_NOTIFICATION_EMAIL=
LEAD_FROM_EMAIL=Salt Lash City <onboarding@resend.dev>

# Reviews sync (npm run sync:reviews)
GOOGLE_PLACES_API_KEY=
GOOGLE_PLACE_ID=
VAGARO_BUSINESS_URL=https://www.vagaro.com/saltlashcity
# Set to 1 to refresh Vagaro via Playwright (requires: npm i -D playwright && npx playwright install chromium)
VAGARO_SYNC=
